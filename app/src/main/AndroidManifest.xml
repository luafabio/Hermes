<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hermes">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">


        <activity
            android:name="com.example.hermes.MainActivity"
            android:label="Alarmas Recientes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.hermes.Main2Activity"
            android:label="Crear una Alarma"
            android:parentActivityName="com.example.hermes.MainActivity"
            tools:ignore="ExtraText">
        </activity>
        <activity
            android:name="com.example.hermes.Main3Activity"
            android:label="Crear una Alarma"
            android:parentActivityName="com.example.hermes.MainActivity">
        </activity>

        <service android:name="com.example.hermes.FcmMensajes">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>